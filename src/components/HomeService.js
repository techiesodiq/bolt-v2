import { Typography } from "@mui/material";
import { Box } from "@mui/system";
import React from "react";

const HomeService = () => {
  return (
    <Box background="#FFFDF6" padding="20px 60px">
      <Typography variant="h3"></Typography>
    </Box>
  );
};

export default HomeService;
